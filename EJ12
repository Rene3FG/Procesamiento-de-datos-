    num1 = int(input("Ingresa el primer número: "))
    num2 = int(input("Ingresa el segundo número: "))
    
    # Asegurar que num1 sea el menor
    if num1 > num2:
        num1, num2 = num2, num1
    
    suma = sum(range(num1, num2 + 1))
    
    print(f"La suma de los números desde {num1} hasta {num2} es: {suma}")
    
    # Mostrar operación (opcional)
    numeros = " + ".join(str(i) for i in range(num1, num2 + 1))
    print(f"{numeros} = {suma}")
