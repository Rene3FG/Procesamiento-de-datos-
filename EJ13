    entrada = input("Ingresa varios números separados por espacios: ")
    numeros = [int(x) for x in entrada.split()]
    
    print("Números mayores a cero:")
    for num in numeros:
        if num > 0:
            print(num)
            
